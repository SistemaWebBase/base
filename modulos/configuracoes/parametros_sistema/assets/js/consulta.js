// Abrir cadastro
function abrirCadastro(usuario, empresa, usuario, token) {
	window.location.href = "cadastro.php?chave=" + chave + "&empresa=" + empresa + "&usuario=" + usuario + "&token=" + token;
}
